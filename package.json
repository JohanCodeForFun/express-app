{
  "name": "express-app",
  "description": "launch dev enviroment with: node --watch --env-file=.env app.js test application with: curl http://localhost:3000",
  "version": "1.0.0",
  "main": "app.js",
  "devDependencies": {},
  "scripts": {
    "dev": "node --watch --env-file=.env app.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.2",
    "express-async-handler": "^1.2.0"
  }
}
